<!-- Views/StatusPanel.xaml -->

<UserControl x:Class="PackItPro.Views.StatusPanel"

             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"

             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"

             mc:Ignorable="d"

             d:DesignHeight="70" d:DesignWidth="800">



    <UserControl.Resources>

        <!-- Spinner storyboard — code-behind calls Begin() / Stop() -->

        <Storyboard x:Key="SpinAnimation" RepeatBehavior="Forever">

            <DoubleAnimation

                Storyboard.TargetName="SpinnerRotation"

                Storyboard.TargetProperty="Angle"

                From="0" To="360"

                Duration="0:0:0.9"/>

        </Storyboard>

    </UserControl.Resources>



    <Grid>

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="Auto"/>

            <!-- Icon -->

            <ColumnDefinition Width="*"/>

            <!-- Message + progress -->

            <ColumnDefinition Width="Auto"/>

            <!-- Percentage -->

            <ColumnDefinition Width="Auto"/>

            <!-- Button -->

        </Grid.ColumnDefinitions>



        <!-- ── Status Icon ────────────────────────────────────────────────────

             Three layers stacked. Visibility driven by DataTrigger styles.

        -->

        <Border Grid.Column="0"

                Width="36" Height="36"

                Background="#1f1f33"

                CornerRadius="18"

                Margin="0,0,14,0"

                VerticalAlignment="Center">

            <Grid>



                <!-- ① Spinner — shown only while IsBusy -->

                <Grid Width="22" Height="22"

                      HorizontalAlignment="Center"

                      VerticalAlignment="Center"

                      RenderTransformOrigin="0.5,0.5">

                    <Grid.Style>

                        <Style TargetType="Grid">

                            <Setter Property="Visibility" Value="Collapsed"/>

                            <Style.Triggers>

                                <DataTrigger Binding="{Binding IsBusy}" Value="True">

                                    <Setter Property="Visibility" Value="Visible"/>

                                </DataTrigger>

                            </Style.Triggers>

                        </Style>

                    </Grid.Style>

                    <Grid.RenderTransform>

                        <RotateTransform x:Name="SpinnerRotation" Angle="0"/>

                    </Grid.RenderTransform>

                    <!--

                        Symmetric 270° open arc drawn with ArcSegment so the gap

                        is always at the same position relative to the geometry centre.

                        The arc sweeps from the 12-o'clock position clockwise 270°,

                        leaving a 90° gap at top-right. RenderTransformOrigin="0.5,0.5"

                        on the parent Grid makes the RotateTransform spin around the

                        visual centre correctly.

                    -->

                    <Path StrokeThickness="2"

                          StrokeStartLineCap="Round"

                          StrokeEndLineCap="Round"

                          Fill="Transparent"

                          Stroke="{StaticResource AppPrimaryColor}"

                          HorizontalAlignment="Center"

                          VerticalAlignment="Center">

                        <Path.Data>

                            <PathGeometry>

                                <PathFigure StartPoint="11,2" IsClosed="False">

                                    <!-- Large arc: radius 9, sweep 270° clockwise -->

                                    <ArcSegment Point="2,11"

                                                Size="9,9"

                                                SweepDirection="Clockwise"

                                                IsLargeArc="True"/>

                                </PathFigure>

                            </PathGeometry>

                        </Path.Data>

                    </Path>

                </Grid>



                <!-- ② Success checkmark — shown when IsSuccess=true AND IsBusy=false -->

                <Viewbox Width="22" Height="22"

                         HorizontalAlignment="Center"

                         VerticalAlignment="Center">

                    <Viewbox.Style>

                        <Style TargetType="Viewbox">

                            <Setter Property="Visibility" Value="Collapsed"/>

                            <Style.Triggers>

                                <DataTrigger Binding="{Binding IsSuccess}" Value="True">

                                    <Setter Property="Visibility" Value="Visible"/>

                                </DataTrigger>

                                <DataTrigger Binding="{Binding IsBusy}" Value="True">

                                    <Setter Property="Visibility" Value="Collapsed"/>

                                </DataTrigger>

                            </Style.Triggers>

                        </Style>

                    </Viewbox.Style>

                    <Canvas Width="20" Height="20">

                        <Path Data="M10 2a8 8 0 1 1 0 16A8 8 0 0 1 10 2Zm3.36 5.65a.5.5 0 0 0-.64-.06l-.07.06L9 11.3 7.35 9.65l-.07-.06a.5.5 0 0 0-.7.7l.06.07 2 2 .07.06a.5.5 0 0 0 .57 0l.07-.06 4-4 .06-.07a.5.5 0 0 0-.06-.64Z"

                              Fill="{StaticResource AppStatusCleanColor}"/>

                    </Canvas>

                </Viewbox>



                <!-- ③ Idle checkmark — shown when NOT busy AND NOT success (default) -->

                <Viewbox Width="22" Height="22"

                         HorizontalAlignment="Center"

                         VerticalAlignment="Center">

                    <Viewbox.Style>

                        <Style TargetType="Viewbox">

                            <Setter Property="Visibility" Value="Visible"/>

                            <Style.Triggers>

                                <DataTrigger Binding="{Binding IsBusy}" Value="True">

                                    <Setter Property="Visibility" Value="Collapsed"/>

                                </DataTrigger>

                                <DataTrigger Binding="{Binding IsSuccess}" Value="True">

                                    <Setter Property="Visibility" Value="Collapsed"/>

                                </DataTrigger>

                            </Style.Triggers>

                        </Style>

                    </Viewbox.Style>

                    <Canvas Width="20" Height="20">

                        <Path Data="M10 2a8 8 0 1 1 0 16A8 8 0 0 1 10 2Zm3.36 5.65a.5.5 0 0 0-.64-.06l-.07.06L9 11.3 7.35 9.65l-.07-.06a.5.5 0 0 0-.7.7l.06.07 2 2 .07.06a.5.5 0 0 0 .57 0l.07-.06 4-4 .06-.07a.5.5 0 0 0-.06-.64Z"

                              Fill="{StaticResource AppStatusCleanColor}"

                              Opacity="0.35"/>

                    </Canvas>

                </Viewbox>



            </Grid>

        </Border>



        <!-- ── Message + Progress Bar ──────────────────────────────────────── -->

        <StackPanel Grid.Column="1" VerticalAlignment="Center">

            <TextBlock Text="{Binding Message}"

                       Foreground="{StaticResource AppTextColor}"

                       FontFamily="Segoe UI"

                       FontSize="13"

                       FontWeight="Medium"

                       Margin="0,0,0,4"/>



            <ProgressBar Height="4"

                         Minimum="0"

                         Maximum="100"

                         Value="{Binding ProgressPercentage}"

                         Foreground="{StaticResource AppPrimaryColor}"

                         Background="{StaticResource AppBorderColor}"

                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"

                         Style="{StaticResource RoundedProgressBarStyle}"/>

        </StackPanel>



        <!-- Progress percentage (IsBusy only) -->

        <TextBlock Grid.Column="2"

                   Text="{Binding ProgressPercentage, StringFormat={}{0:F0}%}"

                   Foreground="{StaticResource AppTextTertiaryColor}"

                   FontFamily="Consolas"

                   FontSize="12"

                   FontWeight="SemiBold"

                   VerticalAlignment="Center"

                   Margin="12,0"

                   Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>



        <!-- Pack Now button -->

        <Button Grid.Column="3"

                Content="Pack Now"

                Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.PackCommand}"

                Style="{StaticResource Win11Button}"

                Height="40"

                FontSize="13"

                FontWeight="SemiBold"

                IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.FileList.HasFiles}"

                MinWidth="120"

                Padding="20,0"

                Cursor="Hand">

            <Button.Effect>

                <DropShadowEffect Color="#4f46e5" Opacity="0.4" BlurRadius="15" ShadowDepth="0" po:Freeze="True"/>

            </Button.Effect>

        </Button>



    </Grid>

</UserControl>

