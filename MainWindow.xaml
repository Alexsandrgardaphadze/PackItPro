<Window x:Class="PackItPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PackItPro"
        mc:Ignorable="d"
        Title="PackItPro" Height="650" Width="1050"
        Background="{StaticResource AppBackgroundColor}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        MinWidth="900" MinHeight="600">

    <Window.Resources>
        <SolidColorBrush x:Key="AppPrimaryColor" Color="#0078D4"/>
        <SolidColorBrush x:Key="AppPrimaryHoverColor" Color="#106EBE"/>
        <SolidColorBrush x:Key="AppPrimaryPressedColor" Color="#005A9E"/>
        <SolidColorBrush x:Key="AppPrimaryDisabledColor" Color="#7A7A7A"/>

        <SolidColorBrush x:Key="AppTextColor" Color="White"/>
        <SolidColorBrush x:Key="AppTextDisabledColor" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="AppTextSecondaryColor" Color="#AAAAAA"/>
        <SolidColorBrush x:Key="AppTextTertiaryColor" Color="#888888"/>
        <SolidColorBrush x:Key="AppTextQuaternaryColor" Color="#666666"/>

        <SolidColorBrush x:Key="AppBackgroundColor" Color="#1E1E2D"/>
        <SolidColorBrush x:Key="AppPanelColor" Color="#2A2A40"/>
        <SolidColorBrush x:Key="AppPanelSecondaryColor" Color="#33334D"/>

        <SolidColorBrush x:Key="AppBorderColor" Color="#4F4F6A"/>
        <SolidColorBrush x:Key="AppStatusCleanColor" Color="#4CAF50"/>
        <SolidColorBrush x:Key="AppStatusErrorColor" Color="#FF5252"/>
        <SolidColorBrush x:Key="AppStatusWarningColor" Color="#FFD54F"/>
        <SolidColorBrush x:Key="AppStatusPendingColor" Color="#FFD700"/>

        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#353545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4F4F6A"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <!-- Ensure menu sizing and font consistency -->
        <Style TargetType="Menu">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{StaticResource AppBackgroundColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
        </Style>


        <Style x:Key="Win11Button" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AppPrimaryColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AppPrimaryHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource AppPrimaryPressedColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{StaticResource AppPrimaryDisabledColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource AppTextDisabledColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource Win11Button}">
            <Setter Property="Background" Value="#353545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#454555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#252535"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#505069"/>
                                <Setter Property="Foreground" Value="#909090"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid Name="TemplateRoot" SnapsToDevicePixels="True">
                            <Border x:Name="ProgressBarTrack" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="3" />
                            <Rectangle x:Name="PART_Track"/>
                            <Decorator x:Name="PART_Indicator" 
                                       HorizontalAlignment="Left" 
                                       VerticalAlignment="Stretch">
                                <Grid>
                                    <Border x:Name="Indicator" 
                                            Background="{TemplateBinding Foreground}" 
                                            CornerRadius="3"/>
                                </Grid>
                            </Decorator>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="TemplateRoot" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Setter TargetName="Indicator" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FileItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="{StaticResource AppPanelSecondaryColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353545"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3A3A50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="{StaticResource AppPanelColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}">
                            <Grid>
                                <ContentPresenter ContentSource="Header" Margin="10,3"/>
                                <Popup x:Name="Popup" IsOpen="{TemplateBinding IsSubmenuOpen}" 
                                       Placement="Bottom" PopupAnimation="Fade">
                                    <Border x:Name="SubMenuBorder" Background="{StaticResource AppPanelColor}" 
                                            BorderBrush="{StaticResource AppBorderColor}" BorderThickness="1" Padding="2">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="{StaticResource AppPanelColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="5,3">
                            <ContentPresenter ContentSource="Header" Margin="10,3"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource AppPanelSecondaryColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style x:Key="SummaryLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AppTextTertiaryColor}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SummaryValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InputControlStyle" TargetType="Control">
            <Setter Property="Background" Value="{StaticResource AppBackgroundColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppBorderColor}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>

        <Style x:Key="SettingLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AppTextSecondaryColor}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="SettingCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource AppTextSecondaryColor}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InfoButtonStyle" TargetType="Button">
            <Setter Property="Content" Value="?"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="{StaticResource AppBorderColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                BorderThickness="1" 
                                BorderBrush="#6F6F8A">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SummarySeparatorStyle" TargetType="Border">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Background" Value="{StaticResource AppBorderColor}"/>
            <Setter Property="Margin" Value="0,15,0,15"/>
        </Style>

        <!-- Ensure ComboBox items follow theme -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource AppPanelColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextColor}"/>
        </Style>

    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="{StaticResource AppBackgroundColor}" Padding="0,6,6,0" Margin="0,0,0,0">
            <MenuItem Header="File" Foreground="{StaticResource AppTextColor}" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="Add Files..." Click="BrowseFiles_Click" InputGestureText="Ctrl+O" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="Clear All Files" Click="ClearAllFiles_Click" InputGestureText="Ctrl+Del" Style="{StaticResource SubMenuItemStyle}"/>
                <Separator Background="{StaticResource AppBorderColor}"/>
                <MenuItem Header="Export Logs..." Click="ExportLogs_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <Separator Background="{StaticResource AppBorderColor}"/>
                <MenuItem Header="Exit" Click="Exit_Click" InputGestureText="Alt+F4" Style="{StaticResource SubMenuItemStyle}"/>
            </MenuItem>
            <MenuItem Header="Settings" Foreground="{StaticResource AppTextColor}" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="Output Location..." Click="SetOutputLocation_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="VirusTotal API Key..." Click="SetVirusApiKey_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="PackItPro Settings..." Click="PackItProSettings_Click" Style="{StaticResource SubMenuItemStyle}"/>
            </MenuItem>
            <MenuItem Header="Tools" Foreground="{StaticResource AppTextColor}" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="Test Package..." Click="TestPackage_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="View Cache..." Click="ViewCache_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="Clear Cache" Click="ClearCache_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <Separator Background="{StaticResource AppBorderColor}"/>
                <MenuItem Header="Export Package List..." Click="ExportList_Click" Style="{StaticResource SubMenuItemStyle}"/>
            </MenuItem>
            <MenuItem Header="Help" Foreground="{StaticResource AppTextColor}" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="Documentation" Click="Documentation_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="GitHub Repository" Click="GitHub_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="Report Issue" Click="ReportIssue_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <Separator Background="{StaticResource AppBorderColor}"/>
                <MenuItem Header="Check for Updates" Click="CheckUpdates_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <Separator Background="{StaticResource AppBorderColor}"/>
                <MenuItem Header="About PackItPro" Click="About_Click" Style="{StaticResource SubMenuItemStyle}"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="0,0,10,0" BorderBrush="{StaticResource AppBorderColor}"
                    BorderThickness="1" CornerRadius="10"
                    Background="{StaticResource AppPanelColor}"
                    x:Name="DropAreaBorder"
                    AllowDrop="True"
                    Drop="DropArea_Drop"
                    DragEnter="DropArea_DragEnter"
                    DragLeave="DropArea_DragLeave">

                <Grid>
                    <StackPanel x:Name="EmptyDropState"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Visible">
                        <TextBlock Text="📦" FontSize="48" HorizontalAlignment="Center" Foreground="{StaticResource AppTextTertiaryColor}"/>
                        <TextBlock Text="Drag and Drop Files Here" FontSize="18" Foreground="{StaticResource AppTextSecondaryColor}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,10,0,5"/>
                        <TextBlock Text="or click to browse" FontSize="14" Foreground="{StaticResource AppTextTertiaryColor}" HorizontalAlignment="Center"/>
                        <Button Content="Browse Files" Style="{StaticResource Win11Button}" Click="BrowseFiles_Click" FontSize="16"  Margin="0,15,0,0" Height="36" Width="150"/>
                        <TextBlock Text="(Maximum 20 files)" FontSize="12" Foreground="{StaticResource AppTextQuaternaryColor}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>

                    <ListView x:Name="FileListView" Background="Transparent" BorderThickness="0" Margin="12" Visibility="Collapsed" ItemContainerStyle="{StaticResource FileItemStyle}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="📄" Width="35" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FileIcon, Mode=OneWay}" Foreground="{StaticResource AppTextColor}" FontFamily="Segoe UI Emoji" HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="File Name" Width="200" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FileName, Mode=OneWay}" Foreground="{StaticResource AppTextColor}" FontFamily="Consolas"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Size" Width="100" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Size, Mode=OneWay}" Foreground="{StaticResource AppTextColor}" FontFamily="Consolas"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Status" Width="120" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding StatusIcon}" Margin="0,0,6,0" Foreground="{StaticResource AppTextTertiaryColor}"/>
                                                <TextBlock Text="{Binding Status, Mode=OneWay}" Foreground="{Binding StatusColor, Mode=OneWay}" FontFamily="Consolas"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Actions" Width="70" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Remove"
                                                    Command="{Binding RemoveCommand}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Height="24" Padding="8,0"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource AppPanelColor}" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="PackItPro Settings" FontSize="16" Foreground="{StaticResource AppTextColor}" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <TextBlock Text="Output File Name:" Style="{StaticResource SettingLabelStyle}"/>
                        <TextBox x:Name="OutputFileNameTextBox" Style="{StaticResource InputControlStyle}" Margin="0,0,0,8" Text="MyPackage"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox x:Name="ScanWithVirusTotalCheckBox" Grid.Column="0" Content="Scan with VirusTotal" Style="{StaticResource SettingCheckBoxStyle}" IsChecked="True" ToolTip="Checks files against VirusTotal database for malware"/>
                            <Button Grid.Column="1" Style="{StaticResource InfoButtonStyle}">
                                <Button.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="Files will be checked against VirusTotal's database to ensure safety. If scanning fails, packaging continues with a warning." TextWrapping="Wrap" MaxWidth="280"/>
                                    </ToolTip>
                                </Button.ToolTip>
                            </Button>
                        </Grid>

                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox x:Name="IncludeWingetUpdaterCheckBox" Grid.Column="0" Content="Include Winget Updater" Style="{StaticResource SettingCheckBoxStyle}" IsChecked="False" ToolTip="Adds update_all.bat script for automatic updates via Windows Package Manager (Winget)"/>
                            <Button Grid.Column="1" Style="{StaticResource InfoButtonStyle}">
                                <Button.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="Adds update_all.bat script for automatic app updates via Windows Package Manager (Winget) after installation completes." TextWrapping="Wrap" MaxWidth="280"/>
                                    </ToolTip>
                                </Button.ToolTip>
                            </Button>
                        </Grid>

                        <Expander x:Name="AdvancedOptionsExpander" Header="Advanced Options" Foreground="{StaticResource AppTextTertiaryColor}" Background="Transparent" BorderThickness="0" Margin="0,12,0,0" IsExpanded="False">
                            <StackPanel Margin="0,12,0,0">
                                <TextBlock Text="Compression" Style="{StaticResource SettingLabelStyle}"/>
                                <ComboBox x:Name="CompressionComboBox" Style="{StaticResource InputControlStyle}" 
                                          Margin="0,0,0,12" 
                                          SelectedIndex="1"
                                          Foreground="{StaticResource AppTextColor}"
                                    Background="{StaticResource AppPanelColor}"
                                    BorderBrush="{StaticResource AppBorderColor}"> 
                                    <ComboBoxItem Content="None (Fastest)"/>
                                    <ComboBoxItem Content="Fast (Recommended)"/>
                                    <ComboBoxItem Content="Maximum (Smallest)"/>
                                </ComboBox>
                                <CheckBox x:Name="UseLZMACompressionCheckBox" Content="Use LZMA Compression" Style="{StaticResource SettingCheckBoxStyle}" Checked="UseLZMACompressionCheckBox_Checked" Unchecked="UseLZMACompressionCheckBox_Unchecked"/>
                                <CheckBox x:Name="OnlyScanExecutablesCheckBox" Content="Only Scan Executables" Style="{StaticResource SettingCheckBoxStyle}" Checked="OnlyScanExecutablesCheckBox_Checked" Unchecked="OnlyScanExecutablesCheckBox_Unchecked"/>
                                <CheckBox x:Name="AutoRemoveInfectedFilesCheckBox" Content="Auto Remove Infected Files" Style="{StaticResource SettingCheckBoxStyle}" Checked="AutoRemoveInfectedFilesCheckBox_Checked" Unchecked="AutoRemoveInfectedFilesCheckBox_Unchecked"/>
                                <CheckBox x:Name="RequireAdminCheckBox" Content="Require administrator rights" Style="{StaticResource SettingCheckBoxStyle}" IsChecked="False" Checked="RequireAdminCheckBox_Checked" Unchecked="RequireAdminCheckBox_Checked">
                                    <CheckBox.ToolTip>
                                        <ToolTip>
                                            <TextBlock Text="Requests administrator privileges for the entire installation process. May be required by some bundled installers." TextWrapping="Wrap" MaxWidth="280"/>
                                        </ToolTip>
                                    </CheckBox.ToolTip>
                                </CheckBox>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="{StaticResource AppPanelColor}" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Summary" FontSize="16" Foreground="{StaticResource AppTextColor}" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <StackPanel Grid.Row="1">
                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Files:" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock Grid.Column="1" x:Name="FileCountTextBlock" Text="0" Style="{StaticResource SummaryValueStyle}"/>
                            </Grid>

                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Total Size:" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock Grid.Column="1" x:Name="TotalSizeTextBlock" Text="0 KB" Style="{StaticResource SummaryValueStyle}"/>
                            </Grid>

                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Safe Files:" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock Grid.Column="1" x:Name="SafeFilesTextBlock" Text="0" Style="{StaticResource SummaryValueStyle}"/>
                            </Grid>

                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Status:" Style="{StaticResource SummaryLabelStyle}"/>
                                <TextBlock Grid.Column="1" x:Name="StatusTextBlock" Text="Ready" Foreground="{StaticResource AppStatusCleanColor}" Style="{StaticResource SummaryValueStyle}"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Background="{StaticResource AppPanelColor}" CornerRadius="8" Padding="15">
                    <Button x:Name="PackButton" Content="Pack Now" Style="{StaticResource Win11Button}" Height="40" FontSize="16" Click="PackNow_Click" IsEnabled="False"/>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2"
                Background="{StaticResource AppPanelColor}"
                CornerRadius="6"
                Padding="12,10"
                Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="4"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <TextBlock x:Name="StatusMessageTextBlock" Text="Ready to pack files. Add files to continue." Foreground="{StaticResource AppTextSecondaryColor}" FontFamily="Consolas" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ProgressPercentTextBlockSecondary" Text="0%" Foreground="{StaticResource AppTextTertiaryColor}" FontFamily="Consolas" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed"/>
                </Grid>

                <!-- Inline progress bar hidden; main progress is at bottom to avoid duplication -->
                <ProgressBar Grid.Row="1" x:Name="ProcessingProgressBar" Height="4" Minimum="0" Maximum="100" Value="0" Foreground="{StaticResource AppPrimaryColor}" Background="Transparent" VerticalAlignment="Bottom" Visibility="Collapsed" Style="{StaticResource RoundedProgressBarStyle}"/>
            </Grid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ProgressBar Grid.Column="0" x:Name="ProcessProgressBar" Height="6" Minimum="0" Maximum="100" Value="0" Foreground="{StaticResource AppPrimaryColor}" Background="#3A3A4D" Style="{StaticResource RoundedProgressBarStyle}"/>
            <TextBlock Grid.Column="1" x:Name="ProgressPercentTextBlock" Text="0%" Foreground="{StaticResource AppTextSecondaryColor}" Margin="8,0,0,0" FontFamily="Consolas" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>