<Window x:Class="PackItPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PackItPro"
        mc:Ignorable="d"
        Title="PackItPro" Height="606" Width="1000"
        Background="#1E1E2F"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinWidth="800" MinHeight="500">

    <Window.Resources>
        <!-- ToolTip Style -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#353545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4F4F6A"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <!-- Windows 11 Style Button -->
        <Style x:Key="Win11Button" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1084D9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#006CC1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#505069"/>
                                <Setter Property="Foreground" Value="#909090"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded ProgressBar Style -->
        <Style x:Key="RoundedProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="5"/>
            <!-- Default height -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid Name="TemplateRoot" SnapsToDevicePixels="True">
                            <!-- Background Border (Track) with CornerRadius -->
                            <Border x:Name="ProgressBarTrack" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="3" />
                            <!-- Set CornerRadius here -->
                            <!-- Foreground Rectangle (Fill) with Clipping -->
                            <Rectangle x:Name="PART_Track"/>
                            <Decorator x:Name="PART_Indicator" 
                                       HorizontalAlignment="Left" 
                                       VerticalAlignment="Stretch">
                                <Grid>
                                    <Border x:Name="Indicator" 
                                            Background="{TemplateBinding Foreground}" 
                                            CornerRadius="3"/>
                                    <!-- Match CornerRadius here -->
                                </Grid>
                            </Decorator>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="TemplateRoot" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Setter TargetName="Indicator" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- File Item Style -->
        <Style x:Key="FileItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="#2C2C3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"/>
                                <Button Grid.Column="1" Content="✕" 
                                        Foreground="#CCCCCC"
                                        Background="Transparent" 
                                        BorderThickness="0"
                                        Padding="5,0"
                                        Margin="5,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353545"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3A3A50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Menu Item Styles -->
        <Style x:Key="MenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="#252536"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}">
                            <Grid>
                                <ContentPresenter ContentSource="Header" Margin="10,3"/>
                                <Popup x:Name="Popup" IsOpen="{TemplateBinding IsSubmenuOpen}" 
                                       Placement="Bottom" PopupAnimation="Fade">
                                    <Border x:Name="SubMenuBorder" Background="#252536" 
                                            BorderBrush="#3F3F5A" BorderThickness="1" Padding="2">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="#252536"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="5,3">
                            <ContentPresenter ContentSource="Header" Margin="10,3"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#353545" TargetName="Border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header Style for GridViewColumn -->
        <Style x:Key="GridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#2C2C3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Menu Bar -->
            <RowDefinition Height="*"/>
            <!-- Main Content Area -->
            <RowDefinition Height="Auto"/>
            <!-- Status Area -->
            <RowDefinition Height="Auto"/>
            <!-- Progress Bar -->
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="#1E1E2F" Padding="0,5">
            <MenuItem Header="File" Foreground="White" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="Clear All Files" Click="ClearAllFiles_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <Separator Background="#3F3F5A"/>
                <MenuItem Header="Exit" Click="Exit_Click" Style="{StaticResource SubMenuItemStyle}"/>
            </MenuItem>
            <MenuItem Header="Settings" Foreground="White" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="Output Location..." Click="SetOutputLocation_Click" Style="{StaticResource SubMenuItemStyle}"/>
                <MenuItem Header="VirusTotal API Key..." Click="SetVirusApiKey_Click" Style="{StaticResource SubMenuItemStyle}"/>
            </MenuItem>
            <MenuItem Header="Help" Foreground="White" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="About" Click="About_Click" Style="{StaticResource SubMenuItemStyle}"/>
            </MenuItem>
        </Menu>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- File List Area -->
            <Border Grid.Column="0" Margin="0,0,10,0" BorderBrush="#3F3F5A" 
                    BorderThickness="1" CornerRadius="8"
                    Background="#252536"
                    x:Name="DropAreaBorder"
                    AllowDrop="True" 
                    Drop="DropArea_Drop"
                    DragEnter="DropArea_DragEnter"
                    DragLeave="DropArea_DragLeave">

                <Grid>
                    <!-- Empty State -->
                    <StackPanel x:Name="EmptyDropState" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                Visibility="Visible">
                        <TextBlock Text="📦" FontSize="48" HorizontalAlignment="Center" Foreground="#6F6F8A"/>
                        <TextBlock Text="Drag &amp; Drop Files Here" 
                                   FontSize="18" 
                                   Foreground="#CCCCCC" 
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center"
                                   Margin="0,10,0,5"/>
                        <TextBlock Text="or click to browse" 
                                   FontSize="14" 
                                   Foreground="#9F9FAF" 
                                   HorizontalAlignment="Center"/>
                        <Button Content="Browse Files" 
                                Style="{StaticResource Win11Button}"
                                Click="BrowseFiles_Click"
                                Margin="0,15,0,0"
                                Height="22"
                                Width="140"
                                FontSize="16"/>
                        <TextBlock Text="(Maximum 20 files)" 
                                   FontSize="12" 
                                   Foreground="#7F7F8F" 
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,0"/>
                    </StackPanel>

                    <!-- File List -->
                    <ListView x:Name="FileListView" 
                              Background="Transparent" 
                              BorderThickness="0"
                              Margin="10"
                              Visibility="Collapsed"
                              ItemContainerStyle="{StaticResource FileItemStyle}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="File Name" Width="200" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FileName, Mode=OneWay}" Foreground="White" FontFamily="Consolas"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Size" Width="100" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Size, Mode=OneWay}" Foreground="White" FontFamily="Consolas"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Status" Width="120" HeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <TextBlock Text="{Binding Status, Mode=OneWay}"
                                                           Foreground="{Binding StatusColor, Mode=OneWay}"
                                                           FontFamily="Consolas"/>
                                            </Grid>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>

            <!-- Configuration & Pack Panel -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Pack Settings -->
                <Border Grid.Row="0" Background="#252536" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Pack Settings" FontSize="16" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <TextBlock Text="Output File Name:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                        <TextBox x:Name="OutputFileNameTextBox" 
                                 Background="#1E1E2F" 
                                 Foreground="White" 
                                 BorderBrush="#3F3F5A"
                                 Padding="8,5" 
                                 Margin="0,0,0,15"
                                 FontFamily="Consolas"
                                 Text="MyPackage"/>

                        <!-- VirusTotal Scan with Help Icon -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox x:Name="ScanWithVirusTotalCheckBox" 
                                      Grid.Column="0"
                                      Content="Scan with VirusTotal" 
                                      Foreground="#CCCCCC" 
                                      IsChecked="True"
                                      VerticalAlignment="Center"/>

                            <Button Grid.Column="1" 
                                    Content="?" 
                                    Width="20" 
                                    Height="20" 
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Background="#3F3F5A" 
                                    Foreground="White" 
                                    BorderThickness="0" 
                                    Cursor="Hand"
                                    HorizontalAlignment="Left"
                                    Margin="8,0,0,0"
                                    ToolTipService.InitialShowDelay="0">
                                <Button.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="Files will be checked against VirusTotal's database to ensure they're safe. If scanning fails, packaging continues with a warning." 
                                                   TextWrapping="Wrap" 
                                                   MaxWidth="280"/>
                                    </ToolTip>
                                </Button.ToolTip>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="10" 
                                                BorderThickness="1" 
                                                BorderBrush="#6F6F8A">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>

                        <!-- Winget Updater with Help Icon -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox x:Name="IncludeWingetUpdaterCheckBox" 
                                      Grid.Column="0"
                                      Content="Include Winget Updater" 
                                      Foreground="#CCCCCC" 
                                      IsChecked="False"
                                      VerticalAlignment="Center"/>

                            <Button Grid.Column="1" 
                                    Content="?" 
                                    Width="20" 
                                    Height="20" 
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Background="#3F3F5A" 
                                    Foreground="White" 
                                    BorderThickness="0" 
                                    Cursor="Hand"
                                    HorizontalAlignment="Left"
                                    Margin="8,0,0,0"
                                    ToolTipService.InitialShowDelay="0">
                                <Button.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="Adds update_all.bat script for automatic app updates via Windows Package Manager (Winget) after installation completes." 
                                                   TextWrapping="Wrap" 
                                                   MaxWidth="280"/>
                                    </ToolTip>
                                </Button.ToolTip>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="10" 
                                                BorderThickness="1" 
                                                BorderBrush="#6F6F8A">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>

                    </StackPanel>
                </Border>

                <!-- Summary Panel -->
                <Border Grid.Row="1" Background="#252536" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Package Summary" FontSize="16" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <!-- Files -->
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <!-- For the label -->
                                <ColumnDefinition Width="*"/>
                                <!-- For the value -->
                                <ColumnDefinition Width="Auto"/>
                                <!-- For potential help icon (if needed later) -->
                            </Grid.ColumnDefinitions>
                            <!-- IMPROVEMENT: Left-align label and add left margin for spacing -->
                            <TextBlock Grid.Column="0" Text="Files:" Foreground="#9F9FAF" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <TextBlock Grid.Column="1" x:Name="FileCountTextBlock" Text="0" Foreground="White"
                       FontFamily="Consolas" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <!-- Grid.Column="2" could be used for a help icon if needed -->
                        </Grid>

                        <!-- Total Size -->
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <!-- For the label -->
                                <ColumnDefinition Width="*"/>
                                <!-- For the value -->
                                <ColumnDefinition Width="Auto"/>
                                <!-- For potential help icon (if needed later) -->
                            </Grid.ColumnDefinitions>
                            <!-- IMPROVEMENT: Left-align label and add left margin for spacing -->
                            <TextBlock Grid.Column="0" Text="Total Size:" Foreground="#9F9FAF" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <TextBlock Grid.Column="1" x:Name="TotalSizeTextBlock" Text="0 KB" Foreground="White"
                       FontFamily="Consolas" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <!-- Grid.Column="2" could be used for a help icon if needed -->
                        </Grid>

                        <!-- Status -->
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <!-- For the label -->
                                <ColumnDefinition Width="*"/>
                                <!-- For the value -->
                                <ColumnDefinition Width="Auto"/>
                                <!-- For potential help icon (if needed later) -->
                            </Grid.ColumnDefinitions>
                            <!-- IMPROVEMENT: Left-align label and add left margin for spacing -->
                            <TextBlock Grid.Column="0" Text="Status:" Foreground="#9F9FAF" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <TextBlock Grid.Column="1" x:Name="StatusTextBlock" Text="Ready" Foreground="#00C176"
                       FontFamily="Consolas" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                            <!-- Grid.Column="2" could be used for a help icon if needed -->
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Pack Button -->
                <Border Grid.Row="2" Background="#252536" CornerRadius="8" Padding="15">
                    <Button x:Name="PackButton" 
                            Content="Pack Now" 
                            Style="{StaticResource Win11Button}"
                            Height="40"
                            FontSize="16"
                            Click="PackNow_Click"
                            IsEnabled="False"/>
                </Border>
            </Grid>
        </Grid>

        <!-- Status Message Area (Redesigned) -->
        <Border Grid.Row="2" 
                Background="#252536" 
                CornerRadius="6" 
                Padding="12,10"
                Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="4"/>
                    <!-- Space for the inline progress bar -->
                </Grid.RowDefinitions>

                <!-- Status Text & Percentage -->
                <Grid Grid.Row="0">
                    <TextBlock x:Name="StatusMessageTextBlock" 
                               Text="Ready to pack files. Add files to continue." 
                               Foreground="#CCCCCC" 
                               FontFamily="Consolas"
                               FontSize="12"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="ProgressPercentTextBlock" 
                               Text="0%" 
                               Foreground="#9F9FAF"
                               FontFamily="Consolas"
                               FontSize="11"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                    <!-- Initially collapsed -->
                </Grid>

                <!-- Inline Progress Bar -->
                <ProgressBar Grid.Row="1" 
                             x:Name="ProcessingProgressBar" 
                             Height="4" 
                             Minimum="0" 
                             Maximum="100" 
                             Value="0" 
                             Foreground="#0078D7" 
                             Background="#3A3A4D"
                             Visibility="Collapsed"  
                    Style="{StaticResource RoundedProgressBarStyle}"/>
                <!-- Apply the rounded style -->
            </Grid>
        </Border>

        <!-- Bottom Progress Bar (Simplified) -->
        <ProgressBar Grid.Row="3" 
                     x:Name="ProcessProgressBar" 
                     Height="6" 
                     Minimum="0" 
                     Maximum="100" 
                     Value="0" 
                     Foreground="#0078D7" 
                     Background="#3A3A4D"
                     Style="{StaticResource RoundedProgressBarStyle}"/>
        <!-- Apply the rounded style -->
    </Grid>
</Window>