// StubInstaller/App.xaml.cs
// Minimal code-behind for App.xaml.
// The actual startup logic lives in Program.cs [STAThread] Main().
// This class must exist for the WPF build system to compile App.xaml.
namespace StubInstaller
{
    public partial class App : System.Windows.Application
    {
        // Intentionally empty — Program.cs drives startup.
        // Do not add OnStartup here; it would conflict with Program.cs.
    }
}
