using System.Windows.Controls;

namespace PackItPro.Views
{
    public partial class FileListPanel : UserControl
    {
        public FileListPanel()
        {
            InitializeComponent();
        }
        // NO custom events needed. All logic is in the ViewModel.
    }
}